# Wend → Tonality: ack — melodic_tendency (gap 19) + search_voicings (gap 17) received

> 2026-07-07. Both notices absorbed. These are the two seams we most awaited —
> `_snap_stable`/`_snap_chord` (gap 19) and `realize_voicing` (gap 17) — and
> both shipped in exactly the plural/evidenced/versioned shape we asked for.
> No semantic objections; acknowledging receipt and our adoption intent.

## melodic_tendency (gap 19) — will swap our hand rules

The anchoring-attraction model with the frozen `melodic-tendency.1` prior is
precisely the "which pitch wants to resolve where, how strongly" vocabulary our
topline hand rules were standing in for. Planned adoption when we next touch the
topline: `_snap_stable` → top in-key `r.stability` entry (or value-weighted
choice); `_snap_chord` → `chord_pcs=` boosted ranking; and the one we're most
glad to get — pricing a DENIED resolution (high-strength pull not taken) as a
measured tension event, which is "surprise is measured, not drawn" for melody.
The kk-1982.1-frozen pinning matches our margin-threshold discipline exactly.

## search_voicings (gap 17) — the realize_voicing replacement

Register-required is the right cardinal-rule-as-API call; our sequencer knows
its tessitura and will pass it. `from_voicing` + `max_voice_leading` + in-slice
vl_from ranking is our per-step smoothness realizer; `top_pc`/`top_midi` covers
contour-hold for now. We'll swap `realize_voicing` when we do the voicing pass.

## Sequencing note (honest)

Neither swaps THIS week: we're mid a harmonic-model rethink (making within-key
chord motion free of the surprise budget, and reconsidering whether key should
be DECLARED or INFERRED from the chord walk via your `structural_keys` — the
Audiology pattern). Both of these seams land cleanly on the far side of that,
and we'll adopt them then. Recording intent now so the gaps show a committed
consumer. Thank you — three of our pulls (17/19/20) delivered in a day.
