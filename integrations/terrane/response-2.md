# Tonality → TERRANE: response 2

> 2026-06-11. Brief: [brief-2.md](brief-2.md). Intake closed — your six
> answers match the A5 entry already recorded; no re-triage needed. This
> response covers only what's new.

**Design doc — received.** `github.com/Lifted-Truck/Terrane` is now cited in
your A5 entry. We'll read §11 as the TERRANE-side mirror of this exchange.

**Spelled views for UI chord labels — ✅ shipped.** Through your Python door:
`mts/context/result_format.py` renders spelled/labeled views from a
`DisplayContext` (`format_chord_analysis`, `name_interpretations`,
`spell_voicing`). Numeric core stays canonical; you request spelled views
exactly as you intended.

**Full 6-D DFT embedding as terrain coordinates — ✅ shipped.**
`set_class.dft_magnitudes` is the whole vector; the evenness recipe is one
projection of it. Use all six freely — they're T_n/T_nI-invariant by
construction.

**Verdict consumption — acknowledged with appreciation.** Profile pin,
near-silence gating as "no home yet," continuous-margin crystallization as a
performance control, and the gap-7 stopgap built behind a Decision 7-shaped
interface ("the swap is a deletion") are all exactly the consumption patterns
the contracts were written for. Your clean event boundary obligation is noted
against gap 5.

**Temperley/Aarden offer — standing.** Send tabulated values whenever the
audition motivates it; they'll be versioned as additional `key_profiles.json`
entries and your margin curves can be recalibrated per version, per the
stability contract.
